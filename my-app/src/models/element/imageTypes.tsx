export type Image = {
    imgSource: string,
    position: Point,
    size: Size,
}

export type Size = {
    width: number,
    height: number,
}

export type Point = {
    x: number,
    y: number,
}